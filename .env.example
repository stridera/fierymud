# FieryMUD Environment Configuration
# Copy this file to .env and adjust values for your environment

# =============================================================================
# Database Configuration (PostgreSQL)
# =============================================================================
POSTGRES_HOST="localhost"
POSTGRES_PORT="5432"
POSTGRES_DB="fierydev"
POSTGRES_USER="muditor"
POSTGRES_PASSWORD="password"

# =============================================================================
# TLS/SSL Configuration
# =============================================================================
# TLS is enabled by default on port 4443. Set paths to your certificates.
# For development, generate self-signed certs:
#   openssl req -x509 -nodes -days 365 -newkey rsa:2048 \
#     -keyout certs/server.key -out certs/server.crt \
#     -subj "/CN=localhost/O=FieryMUD/C=US"
#   openssl dhparam -out certs/dhparams.pem 2048

# Certificate file (fullchain for production)
TLS_CERTIFICATE_FILE="certs/server.crt"

# Private key file
TLS_PRIVATE_KEY_FILE="certs/server.key"

# Diffie-Hellman parameters file
TLS_DH_PARAMS_FILE="certs/dhparams.pem"

# For Let's Encrypt production certificates:
# TLS_CERTIFICATE_FILE="/etc/letsencrypt/live/fierymud.org/fullchain.pem"
# TLS_PRIVATE_KEY_FILE="/etc/letsencrypt/live/fierymud.org/privkey.pem"
# TLS_DH_PARAMS_FILE="/path/to/dhparams.pem"

# =============================================================================
# Logging Configuration
# =============================================================================
# Log level: trace, debug, info, warn, error, critical
LOG_LEVEL="info"

# Directory for log files
LOG_DIRECTORY="logs"

# =============================================================================
# Redis Configuration (for Muditor event bridge)
# =============================================================================
# Option 1: Full Redis URL
# REDIS_URL="redis://localhost:6379"

# Option 2: Individual settings
REDIS_HOST="localhost"
REDIS_PORT="6379"
# REDIS_PASSWORD=""

# Enable/disable event publishing to Redis (for Muditor integration)
FIERYMUD_EVENTS_ENABLED="true"

# =============================================================================
# Admin API Configuration
# =============================================================================
# Secret token for admin API authentication (generate a secure random string)
# FIERYMUD_ADMIN_TOKEN="your-secure-token-here"
