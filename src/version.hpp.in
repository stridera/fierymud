#pragma once
#include <string_view>

// This file is processed by CMake and generates version.hpp.

constexpr std::string_view get_git_hash() { return "@GIT_HASH@"; }
constexpr unsigned int get_build_number() { return @FIERYMUD_BUILD_NUMBER@; }
