name: Build

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-cmake@v1
        with:
          build-version: 3.22.0
      - run: cmake .
      - run: make
